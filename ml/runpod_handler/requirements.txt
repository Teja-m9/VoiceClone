# Pin to versions compatible with your CUDA base image.
runpod==1.7.7
requests==2.32.3
torch==2.4.1
torchaudio==2.4.1
demucs==4.0.1
soundfile==0.12.1
numpy==1.26.4
# Seed-VC is installed from source in the Dockerfile (no PyPI package).
